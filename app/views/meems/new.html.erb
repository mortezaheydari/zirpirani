 <div class="row MarginTop">
    <div class="large-12 columns">
        <div class="panel">
        <h1>New meem</h1>

        <%= render 'form' %>

        <%= link_to 'Back', meems_path %>
        </div>
    </div>
</div>