<% custom_image ||= nil %>
    <div class="large-4 pull-8 columns">
        <div class="panel">
            <% if custom_image.nil? %>
                <%= link_to home_meme_info_path do %>
                    <%= image_tag "poolvadeh.jpeg" %>
                    <h3>ویا تو میمولوژی</h3>
                <% end -%>
            <% else %>
                <%= image_tag custom_image %>
            <% end -%>
        </div>
    </div>